# PLAN.md

项目实施计划。与 `AGENT.md` 配套使用：`AGENT.md` 讲约定，`PLAN.md` 讲进度。

**用法**：完成一条勾一条，并在 `AGENT.md`「当前进展」同步摘要。
新 session 开工先看本文件的「下一个待办」。

图例：`[ ]` 未开始 · `[~]` 进行中 · `[x]` 已完成

---

## 阶段一 · 开题

**目标**：完成总体规划，配置好必要运行环境，接口骨架可跑通。

### 1.1 总体规划

- [x] 技术栈选型与论证
- [x] 分层架构设计
- [x] 架构决策记录（ADR 0001–0004）
- [x] 仓库初始化与目录骨架
- [x] 编写 `AGENT.md` 与本文件

### 1.2 运行环境

- [x] 安装 uv，锁定 Python 3.11（`.python-version`）
- [x] 主干依赖安装并生成 `uv.lock`
- [x] 排查本机 GPU（RTX 4060 Laptop / 8GB，满足克隆需求）
- [ ] 编写 `scripts/setup.ps1` 一键环境脚本
- [ ] 编写 `scripts/download_models.py` 模型下载脚本骨架

### 1.3 接口骨架

> 2026-09-02 代码清除后重置。旧实现的参考在 git 快照 `1c6e868`。

- [x] 共享契约层 `server/schemas.py`
- [x] 音频句柄仓库 `server/session_store.py`
- [ ] 效果器注册表 `server/core/registry.py`
- [ ] 基础效果：倒放、增益、倍速（变调/不变调）、归一化
- [ ] 音频 I/O：录音器、播放器、文件读写
- [ ] `server/main.py` 服务入口与静态资源托管
- [ ] 路由 `/api/audio`：列表、上传、录音起停、播放、下载、删除
- [ ] 路由 `/api/effects`：效果清单、施加效果
- [ ] 路由 `/api/analysis`：频谱
- [ ] Vue 3 + TypeScript + Vite 前端工程初始化（`web/`，见 ADR 0005）
- [ ] 前后端联调打通：能录音 → 能看到波形 → 能播放

### 1.4 开题交付物

- [ ] 开题报告
- [ ] 架构设计说明书（可由 `AGENT.md` + ADR 整理）
- [ ] 可运行的系统原型

**下一个待办**：`server/core/registry.py` 效果器注册表。

---

## 阶段二 · 中期

**目标**：完成软件基本功能 —— 采集、处理、显示、播放，以及基本语音控制。

### 2.1 采集

- [ ] 录音参数可选（采样率 16k/44.1k/48k、单/双声道）
- [ ] 录音电平实时可视化（WebSocket 推送 + Vue 组件）
- [ ] 定时录音自动停止
- [ ] 设备选择与异常提示（无麦克风时不崩）

### 2.2 显示

- [ ] 时域波形图（Canvas，支持缩放与选区）
- [ ] 频域幅度谱（FFT）
- [ ] 语谱图（STFT 热力图）
- [ ] 音频基本信息展示（时长、采样率、声道、峰值）

### 2.3 处理

- [ ] 倒放
- [ ] 增益 / 归一化
- [ ] 倍速播放：变调版（重采样）
- [ ] 倍速播放：不变调版（OLA 时域压扩）
- [ ] 淡入淡出
- [ ] 简易降噪（谱减法）
- [ ] 效果链：多个效果按顺序施加，可查看处理历史
- [ ] 处理前后 A/B 对比播放

### 2.4 播放

- [ ] 播放/暂停/停止
- [ ] 进度拖动与实时播放指针
- [ ] 变速播放

### 2.5 工程质量

- [ ] `tests/` 覆盖 Core 层纯函数
- [ ] 处理历史与撤销
- [ ] 单元测试 + 冒烟脚本纳入提交前检查

**中期验收**：现场演示「录音 → 看波形 → 加效果 → 对比播放」全链路无异常。

---

## 阶段三 · 结题

**目标**：完成拓展功能（语音克隆），系统稳定，产出答辩材料。

### 3.1 语音克隆子服务

- [ ] 独立虚拟环境（Python 3.11 + PyTorch + CUDA）
- [ ] GPT-SoVITS 模型权重下载与环境验证
- [ ] 子服务 HTTP 接口：上传参考音频 → 提交文本 → 返回合成音频
- [ ] 参考音频录制与切片（≥3 秒清晰人声）
- [ ] 主干侧 `TTSProvider` 适配器对接
- [ ] 前端克隆面板：录音/上传参考音、输入文本、试听、导出
- [ ] 结果正确性验证（主观听感 + 客观指标，如说话人相似度）

### 3.2 系统集成

- [ ] 克隆结果回流到主音频列表，可继续施加效果
- [ ] 全局异常处理与友好报错
- [ ] 启动脚本一键拉起前后端

### 3.3 交付物

- [ ] 结题报告
- [ ] 用户使用说明
- [ ] 演示视频或现场演示
- [ ] 代码仓库整理与清理

**结题验收**：语音克隆结果正确可靠，满足题目表拓展功能要求。

---

## 里程碑时间对照

| 阶段 | 核心交付 | 判定标准 |
|---|---|---|
| 开题 | 规划 + 环境 + 接口骨架 | 前后端能跑通一次完整链路 |
| 中期 | 采集/处理/显示/播放 | 现场全链路演示无异常 |
| 结题 | 语音克隆 | 克隆音频可辨识且稳定复现 |

---

## 范围外（明确不做）

- 语音识别（ASR）
- 语音合成（通用 TTS，仅做克隆所需的合成环节）
- 语音端点检测（VAD）
- 语音助手
- 深度伪造检测

如要调整范围，先在 `docs/adr/` 补决策记录。
