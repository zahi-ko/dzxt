# 2026-09-02 · 契约层

## 本次做了什么

- 落地契约层（AGENT.md 待完成第一条），直接提交 master（`d81bd10`，用户授权免分支）：
  - `server/schemas.py`：唯一接口真相源。覆盖音频元数据（AudioMeta/AudioListResponse）、录音（RecordStartRequest/RecordStatusResponse）、效果（EffectInfo/EffectListResponse/ApplyEffectRequest/ApplyEffectResponse）、频谱（SpectrumRequest/SpectrumResponse）、波形包络（WaveformResponse）、音频统计（AudioStatsResponse）、ErrorResponse。常量 `DEFAULT_SAMPLE_RATE=16000`、`SUPPORTED_SAMPLE_RATES`。
  - `server/session_store.py`：线程安全 audio_id 句柄仓库（铁律二）。`AudioEntry`（含 channels/duration 派生属性与 `to_meta()`）+ `SessionStore`（put/get/list/delete/replace，RLock）+ `get_store()` 全局单例 + `monotonic()` 工具。
- 相比旧快照（`1c6e868`）的唯一契约改动：新增 `AudioStatsResponse`——旧 stats 路由返回裸 dict 违反 AGENT.md §7「禁止裸 dict」，后续重写路由时用它修正。
- 同步更新 PLAN.md（勾选两项，下一个待办改为 registry）与 AGENT.md（当前进展、变更日志）。

## 验证

- `uv run ruff check .` 通过
- 冒烟：`get_store().put()` → `to_meta().model_dump_json()` 输出正确

## 遗留与坑

- `server/__init__.py` 与 `server/api/`、`server/core/` 各包尚无 `__init__.py`，本次导入能成功（Python 3 命名空间包），但为稳妥可在后续 session 统一补上。
- session_store 内存态，重启即失；持久化是明确的扩展点，上层 API 不受影响。
- 效果器参数模型的解析（get_type_hints）要求效果文件**不要** `from __future__ import annotations`，写效果时注意（AGENT.md 已有提醒）。

## 下个 session 从哪接手

PLAN.md 阶段一 1.3 下一项：`server/core/registry.py` 效果器注册表，随后基础效果（倒放/增益/倍速/归一化）。旧实现参考 `git show 1c6e868:server/core/registry.py`。
