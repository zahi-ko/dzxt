# 2026-09-09 前端布局与 UI 重构

## 本次做了什么

仅动 `web/src/`，零逻辑改动、零接口契约变更：

- **App.vue 重构**：
  - 拓展功能改为底部三张卡片（默认收起防拥挤）：语音克隆（带在线/离线徽标，点击开侧滑抽屉）、录音质量检测、语音加噪与降噪（后两张禁用态「开发中」，对应阶段三 3.2/3.3）。
  - 克隆抽屉：右侧滑出 400px 抽屉承载 ClonePanel，合成成功自动关闭并回流音频列表。
  - 频谱 / 语谱图合并为标签页面板（v-show 切换，Canvas 保持存活），波形面板标题内嵌选区时间提示。
  - 顶栏品牌化：渐变图标 + 副标题 + 连接状态胶囊（dot + pill）。
- **style.css 全面翻新**：新配色变量（保留旧变量名，所有组件自动继承）、渐变 primary 按钮、focus-visible 焦点环、自定义滚动条、面板标题渐变强调条。
- **组件精修**（仅样式/模板微调，脚本逻辑未动）：RecorderCard 录音按钮红点脉冲、电平条内阴影；AudioLibrary 列表选中态左侧光条；PlayerBar 播放键加粗。

## 验证

- `uv run ruff check .` 通过；`uv run pytest` 82 项全过（Python 未动）。
- `cd web && npm run build` 通过（50 modules，1.66s）。
- AGENT.md 变更日志已记一行。

## 视觉二次重设计（同日，用户要求「新颖、优雅」）

第一版深色蓝灰主题被否后改为「编辑级录音室」浅色主题：

- 配色：暖纸底 `#f4f1ea` + 墨色 `#25221c` + 朱红 `#c9401c`；三个画布保留深底琥珀「示波器屏幕」质感，与浅色页面形成硬件控制台对比。
- 排版：衬线刊头（墨块「声」字标 + Georgia/宋体大字标）、顶栏与拓展区用 2px 粗墨分隔线、拓展卡片改衬线斜体编号 01/02/03、分析标签改下划线式、按钮改细线幽灵风 + 墨块主按钮。
- 技术要点：画布配色全部改读 `--scope-*` CSS 变量（Waveform/Spectrum/SpectrogramCanvas 各加一个 `scope()` helper），以后换主题只动 style.css，画布组件零改动。

## 下一个 session 从哪里接手

- 阶段三拓展功能（3.2 质量检测 / 3.3 加噪降噪）实现后，把 App.vue 底部对应卡片的 `disabled` 去掉、挂上真实面板即可（抽屉/卡片模式可直接复用）。
- 3.1 遗留：参考音频录制与切片（ClonePanel 录音版参考音）。
