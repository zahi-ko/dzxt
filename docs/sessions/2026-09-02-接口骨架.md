# 2026-09-02 · 接口骨架（PLAN.md 1.3 收官）

## 本次做了什么

以快照 `1c6e868` 为基础恢复实现，并适配上一 session 落地的新契约层，完成 PLAN.md 1.3 全部条目：

- **server/core**：`registry.py` 效果器注册表；`effects/basic.py` 基础效果（倒放、增益、变调倍速、不变调倍速 OLA、归一化）；`io/device.py` 录音器与播放器；`io/audio_file.py` WAV 读写；`analysis/spectrum.py` 频谱与波形包络
- **server/api**：`deps.py` + 三个路由（audio / effects / analysis），其中 analysis 的 stats 端点改为返回 `AudioStatsResponse`（修正旧实现裸 dict 问题）
- **server/main.py**：FastAPI 入口，CORS（5173）、静态托管 `web/dist`
- **web/**：Vue 3 + TypeScript + Vite 工程（ADR 0005），`api.ts` 镜像 schemas.py（含新增的 `AudioStatsResponse`），5 个组件（录音卡、音频库、波形、频谱、效果面板）
- **tests/**：29 项测试（效果纯函数 + API 全路由），相对旧版重构了上传辅助函数并新增 stats、效果覆盖句柄两个用例

## 验证记录（2026-09-02 实测）

- `uv run ruff check .` 全过；`uv run pytest -q` 29 passed
- `npm run build`（vue-tsc --noEmit + vite build）通过
- 真实麦克风联调（本机 AMD Microphone Array）：定时录音 1.5s → `/peaks` 波形 → `/analysis/spectrum` → `/stats` → `play`（204）→ gain 效果（新句柄，label「录音 → 音量增益」）→ 下载 WAV（48300 bytes）→ `GET /` 托管 dist（200）
- Vite dev 代理：`localhost:5173/api/health` 经代理转发到 8000 成功

## 遗留问题与坑

- **端口 8000 有遗留服务**：本 session 结束时有一个先前 session 启动的 uvicorn 实例仍在运行（带 reload）。下次开工如遇端口占用，先 `netstat -ano | findstr :8000` 找 PID 结束它
- **curl 走了系统代理**：本机设置了 HTTP 代理，访问 localhost 须加 `--noproxy '*'`，否则 502
- 旧快照恢复的 4 个文件有适配改动（device.py / spectrum.py / analysis.py 路由 / test_api.py），其余与 `1c6e868` 完全一致，考古时可对照
- `web/dist` 已构建但按 .gitignore 不入库，部署时需在目标机重新 `npm run build`

## 下一个 session 从哪里接手

1. 1.2 收尾：`scripts/setup.ps1` 一键环境脚本、`scripts/download_models.py` 骨架（PLAN.md「下一个待办」）
2. 1.4 开题交付物：开题报告、架构设计说明书（AGENT.md + ADR 整理）、可运行系统原型（已具备）
3. 阶段二功能开发按 PLAN.md 2.1–2.5 推进，录音电平 WebSocket 推送是第一项
