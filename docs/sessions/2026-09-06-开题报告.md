# 2026-09-06 开题报告（重做）

## 本次完成（1.4 第一项）

按附件模板 `开题报告（模板）.docx` 重新生成 `prod/report_work/开题报告（第14组）.docx`，覆盖 ADR 0006 三项拓展功能。

### 工程做法

- 复制模板到 `prod/report_work/template.docx`；用 docx skill 的 unpack → 编辑 XML → pack 流程
- 因模板为单一 `<w:tbl>` 大表且需大量整段重写，权衡后用 lxml 树级一次性填充（`fill_report.py`），表头 / 题目 / 打勾 / 五节正文 / 图均在脚本中显式列出，文本与表结构解耦，便于后续微调文案
- 架构框图用 Pillow 黑白灰三层分层绘制（`draw_arch.py`，1724×1180 @300dpi ≈ 14.6×10.0 cm，300ppi 下字号 7.5–16pt），通过 DrawingML `<w:drawing>` 内联至第 4 节文末，附「图1」图注

### 内容结构

| 节 | 字数 | 备注 |
|---|---|---|
| 1. 选题依据及意义 | 309 | ≥300 ✓ |
| 2. 研究现状及发展态势 | 366 | ≥300 ✓ |
| 3. 课题研究内容、关键问题与最终目标 | 664 | ≥500 ✓ |
| 4. 技术路线、实施方案与工具 | 660 | ≥500 ✓ |
| **正文合计** | **1999** | 落在 1500–2000 区间 |

- 表头：学号 cell（×3 行）填「（待补）」占位、姓名 cell 三人「张翔（组长）/ 王仁智 / 叶绍文」、专业 cell 「（待补）」，题目 cell 填「语音处理系统设计与实现（第 14 组 · 团队题）」+ 第二段「指导教师：张彪　　成果形式：软件」
- 题目来源：☑1.工程技术；成果形式：☑3.软件
- 第 4 节附图 1：语音处理系统总体架构框图（前/后端 / 核心处理 / 克隆子服务四层 + 双向数据流箭头）
- 参考文献 6 篇：赵力 / Oppenheim & Schafer / Boll 1979 / Lim & Oppenheim 1979 / GPT-SoVITS GitHub / ITU-T P.56

### 工具链

- 运行环境：`C:\Users\zahi\.workbuddy\binaries\python\envs\default`（3.13.12 + lxml + defusedxml + python-docx）
- 调用：`uv run ruff check .` 通过（prod/ 已 gitignore，不在 lint 范围）
- 填表脚本：`prod/report_work/fill_report.py`（含字数自检输出）
- 绘图脚本：`prod/report_work/draw_arch.py`
- 验证脚本：`prod/report_work/verify_report.py`（用 python-docx 读回检查关键 cell 内容）

### 已知遗留 / 待用户确认

1. **学号 / 专业** cell 留「（待补）」，需组员自填后手动替换（学号从学校系统获取）
2. **导师审查意见** cell 按模板保留空白，提交后由指导教师手签
3. **正文字数** 1999 字紧贴 2000 上限，若评审要求更宽裕可微调任意一节减 5–10 字
4. **PDF 视觉目检** 受限于本机未装 LibreOffice/Word，未能直接生成 PDF；已用 python-docx 读回全部 cell 文本确认内容正确；建议作者最终在 Word 中打开做一次页码 / 分页目检

## 工程事项

- `prod/report_work/` 下文件不入库（`.gitignore:38 prod/`）
- 复现：`uv run python prod/report_work/fill_report.py` 重生成即可（依赖 venv lxml）
- 旧版 `prod/开题报告（第14组）.docx` 与生成脚本已被本次重做替代，原 `arch_diagram.png` 重新绘制以匹配新架构图

## 下一个 session 接手点

1.4 第二项 **开题汇报 PPT**（可用 `python-pptx` 或 docx-skill 同样思路，可视化版式可参考框架图与节标题结构）→ 1.4 第三项 架构设计说明书（直接整理 AGENT.md 第 1–8 节 + ADR 0001–0006）→ 1.4 最后一项 系统原型打包（提交 `web/dist/` + 后端代码 + 安装说明）。
